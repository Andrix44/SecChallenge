[package]
name = "symbolism"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[profile.release]
opt-level = 1
strip = true

[dependencies]
hex = "0.4.3"
